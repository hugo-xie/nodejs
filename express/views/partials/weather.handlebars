<div class="weatherWidget">
  {{#each particals.weather.locations}}
    <div class="location">
      <h3>{{name}}</h3>
      <a href="{{forecastUrl}}">
        <img src="{{iconUrl}}" alt="{{weather}}" />
        {{weather}},{{temp}}
      </a>
    </div>
    {{/each}}
    <small>Source:<a href="http://wwww.wunderground.com">waether underground</a></small>
</div>
